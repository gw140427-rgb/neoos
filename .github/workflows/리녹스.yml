name: Linux Test

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Check Linux
        run: |
          uname -a
          cat /etc/os-release
